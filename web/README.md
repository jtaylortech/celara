# Celara Web

Marketing site and documentation at [celara.dev](https://celara.dev).

```bash
npm install
npm run dev    # http://localhost:3000
npm run build  # Production build
```

Deploys automatically to Vercel on push to main.
