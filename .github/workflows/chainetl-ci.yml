name: ChainETL CI

on:
  push:
    paths: ['chainetl/**']
  pull_request:
    paths: ['chainetl/**']

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12', '3.13']

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4
        with:
          version: 'latest'

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        working-directory: chainetl
        run: uv sync --dev

      - name: Lint
        working-directory: chainetl
        run: uv run ruff check .

      - name: Type check
        working-directory: chainetl
        run: uv run mypy .

      - name: Test
        working-directory: chainetl
        run: uv run pytest -v --cov=src/chainetl --cov-report=term-missing --cov-fail-under=75
